#include <cs50.h>
#include <stdio.h>

#define CHARS_MAX 30
#define ITEMS_MAX 30

struct Menu
{
    char    Item[CHARS_MAX];
    float   Price;
};

int main(void)
{
    struct Menu snackBar[ITEMS_MAX];
      
}